---
- hosts: localhost
  connection: local
  vars:
    user_home: "/home/carlos"
    user_name: "carlos"
  become: yes

  tasks:
    - name: Add PPAs
      apt_repository:
        repo: "{{ item }}"
        state: present
      with_items:
        - "ppa:neovim-ppa/unstable"
        - "ppa:aslatter/ppa"

    - name: Install packages
      package:
        name: 
          - htop
          - tmux
          - neovim
          - i3
          - polybar
          - cmatrix
          - xss-lock
          - alacritty

    - name: Copy bashrc
      copy:
        src: files/bashrc
        dest: "{{ user_home }}/.bashrc"
        owner: "{{ user_name }}"
        group: "{{ user_name }}"

    - name: Copy config files
      copy:
        src: files/config/
        dest: "{{ user_home }}/.config/"
        owner: "{{ user_name }}"
        group: "{{ user_name }}"

    - name: Create a universally accessible log directory
      file:
        path: /var/applogs
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Configure logrotate for applogs.log
      copy:
        src: files/logrotate_applogs
        dest: /etc/logrotate.d/applogs
        owner: root
        group: root
        mode: 0644

    - name: Add Ansible user
      user:
        name: ansible
        system: yes

    - name: Add Ansible user to sudoers
      copy:
        src: files/sudoer_ansible
        dest: /etc/sudoers.d/ansible
        owner: root
        group: root
        mode: 0440

    - name: Add Ansible-pull cron job
      cron:
        name: "Ansible-pull cron job"
        user: ansible
        minute: "*/10"
        job: "ansible-pull -o -U https://github.com/cgracia/ansible_desktop.git >> /var/applogs/ansible_dotfiles.log 2>&1"
